
# Core
streamlit>=1.33
pandas
numpy
pillow

# OpenCV: use headless build on servers
opencv-python-headless

# TensorFlow CPU (no GPU on Streamlit Cloud)
tensorflow>=2.12,<3

# Live webcam via WebRTC
streamlit-webrtc==0.64.5
# Pin PyAV to a stable version (newer wheels can sometimes segfault on some Linux
# images). If you still see crashes, try av==10.0.0 as a fallback.
av==13.1.0
